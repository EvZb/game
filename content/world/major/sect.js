(function () {
    //the base of a dedicated organization with limited entry (bedding,housing,training,tasks,ranking,competitions,trading)
    switch(tmp){
        case x:
            t=`You approuch the gate of the sect. Outsiders are not allowed inside. ${chunk[loc()].arr(0)}.`
            n=`\${btn('leave',"tmpr(x,10,'out');next(w+'terrain/${chunk[loc()].arr(0)}')")}`
            break;
    }
    end()
})()