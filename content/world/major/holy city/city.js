(function () {
    switch(tmp){
        case x:
            t=`You approach the gate of the holy city. Outsiders are not allowed inside.`
            n=`\${btn('leave',"w='world/';tmpr(x,10,'out');next(w+'terrain/${chunk[loc()].arr(0)}')")}`
            break;
    }
    end()
})()