(function () {
    $t = `You are at the local office of the worldly sect. Outsiders can trade or take up work here.`
    $n = `\${btn("beast hunting", "$tmp='bhi';next('bhunt')")} | \${btn("ghost hunting", "next('ghunt')")} | \${btn("leave", "next($loc)")}`
    end()
})()