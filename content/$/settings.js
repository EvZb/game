(function () {let $0=`Text size: \${btn('up',"sett(0,+setting[0]+1);elm('html').style.fontSize=setting[0]+'px'")} | \${btn('down',"sett(0,setting[0]-1);elm('html').style.fontSize=setting[0]+'px'")}`
    fun('menu',$0)
    end(0)
})()