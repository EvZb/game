(function () {let $0=loc()[1];
    $loc = "wild";$t = `You are in the wildlands. The path splits off into two directions.`;$n = `\${btn("leave", "$loc='WP0';next('wildpath')")} \${btn("forward",";next('wildland')")}`
    end()
})()