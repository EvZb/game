(function () {
    $t = `You are at the local office of the worldly sect. Outsiders can trade or take up work here.`
    $n = `\${btn("beast hunting", "$tmp='bhi';next('v/bhunt')")} | \${btn("ghost hunting", "next('v/ghunt')")} | \${btn("leave", "next('vill')")}`
    end()
})()