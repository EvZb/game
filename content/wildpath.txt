(function SCRIPT() {let $0=Number($loc.slice(-1));let $1=`You are on a path that leads to the wildlands. `;let $2=`${btn("toward city","$loc='WP"+($0+1)+"';time('0:0:4:0');next('wildpath')")}`;let $3=`${btn("toward wildlands","$loc='WP"+($0-1)+"';time('0:0:4:0');next('wildpath')")}`
if($0==9){nav("nav",`${btn("village","next('vill')")+' | '+$3}`);nav("txt",`${$1}There is a village nearby.`)}else
if($0==0){nav("nav",`${$2+' | '+btn("wildlands","next('wildland')")}`);nav("txt",`You are outside the wildlands. You can feel its miasma from here.`)}else{nav("txt",`${$1}`);nav("nav",`${$2+' | '+$3}`)}
ELM("script2").remove()})()